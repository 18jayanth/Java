public class Java_94_Hashing_in_Java {
    public static void main(String[] args) {
        //hashing->Technique to convert key,value pairs to indices
        /*
        we will divide each number say num%(length of array) and keep it at the indices
        //so searching will be fast
        //but there can be collisions in one index two or more elements wants to be there
        solution-1 open addressing: if that particular place not there got to next empty space
        solution-2 chaining:don't insert anything at that space but a pointer to a linked list
        which contains the values

        //common classes ,hashmap,hashset,hashtable,linkedhashmap

         */
    }
}
